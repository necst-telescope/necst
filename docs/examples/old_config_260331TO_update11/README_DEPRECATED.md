# Historical migration directory

This directory is retained as a migration record from the older `old_config_260331TO_update11` examples.

For current NANTEN2-style multibeam examples, use:

```text
docs/examples/NANTEN2_multibeam_260331TO/
```
